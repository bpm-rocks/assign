[package]
name=assign
version=1.0.0
include=libassign
description=Return strings and arrays from functions. Stores the values in the given variable name.

[dependencies]
0=is

[devDependencies]
0=embed-docs
1=unittest

[scripts]
readme=embed-docs README.md libassign
test=unittest test/*.test
